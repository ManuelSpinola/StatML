# mod_rf_clas.R — Placeholder
#' @noRd
mod_rf_clas_ui <- function(id) {
  ns <- NS(id)
  div(class = "p-3", h4("Random Forest Clasificación — En desarrollo"))
}
#' @noRd
mod_rf_clas_server <- function(id) {
  moduleServer(id, function(input, output, session) {})
}
