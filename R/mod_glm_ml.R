# mod_glm_ml.R — Placeholder
#' @noRd
mod_glm_ml_ui <- function(id) {
  ns <- NS(id)
  div(class = "p-3", h4("Clasificación GLM — En desarrollo"))
}
#' @noRd
mod_glm_ml_server <- function(id) {
  moduleServer(id, function(input, output, session) {})
}
